待实现：

- [ ] 实现`_sbrk()`（带 heap/stack 撞线检查）,把`malloc`和`free`统一封装成 `app_malloc/app_free`
- [ ] 重定义HAL_Delay(弱定义函数),使用正点原子提供的延时函数，实现delay 和FreeRTOS共用 SysTick 定时器

---

## 项目改进建议

### 1. 代码规范
- [ ] 添加 `.clang-format` 文件统一代码风格
- [ ] 考虑添加静态分析工具（如 `cppcheck`）到 CI 流程

### 2. 构建系统增强
- [ ] 在 CMakeLists.txt 中添加版本信息（PROJECT_VERSION_MAJOR/MINOR）
- [ ] 添加 `make clean` / `make flash` 等便捷脚本
- [ ] 添加 Debug/Release 构建配置分离

### 4. FreeRTOS 集成建议
- [ ] 使用 FreeRTOS 的 CMake 原生支持
- [ ] 配置 `FreeRTOSConfig.h` 时注意堆大小（F407 有 192KB SRAM）
- [ ] 实现线程安全的 `printf`（使用互斥锁）

### 5. 错误处理机制
- [ ] 添加统一的错误码定义（error_code.h）
- [ ] 实现 `HardFault_Handler` 的调试输出（打印寄存器值，便于定位崩溃原因）

### 6. 安全性
- [ ] 如果涉及网络通信（MQTT），使用 TLS 加密
- [ ] 敏感配置（WiFi 密码、密钥）不要硬编码，考虑使用外部配置文件或加密存储

### 7. 文件命名规范
- [ ] 统一文档命名风格（建议全部使用中文或全部使用英文）
- [ ] 源文件/头文件使用小写 + 下划线命名（如 `gpio_driver.c`）
- [ ] 避免文件名中使用特殊字符和空格

---

## CubeMX 生成代码清理

### 1. HAL 模块裁剪（高优先级）
> 文件：`firmware/app/inc/stm32f4xx_hal_conf.h`

项目当前只使用了 GPIO 和 UART，但 CubeMX 默认启用了 20+ 个 HAL 模块（ADC、CAN、CRC、CRYP、DAC、DCMI、ETH、NAND、NOR、HASH、I2C、I2S、IWDG、SD、SPI、TIM、WWDG、PCD、HCD 等）。

- [ ] 注释掉所有未使用的 `HAL_xxx_MODULE_ENABLED` 宏
- [ ] 仅保留：`GPIO`、`UART`、`RCC`、`FLASH`、`CORTEX`、`PWR`、`DMA`
- [ ] 后续新增外设时再按需启用对应模块

### 2. 中断处理函数清理（高优先级）
> 文件：`firmware/app/src/stm32f4xx_it.c`

- [ ] 删除空壳中断处理函数（`NMI_Handler`、`SVC_Handler`、`DebugMon_Handler`、`PendSV_Handler`），启动文件中已有 `[WEAK]` 默认实现
- [ ] 改进 Fault 处理函数（`HardFault_Handler`、`MemManage_Handler`、`BusFault_Handler`、`UsageFault_Handler`），当前是 `while(1)` 死循环，无法诊断问题。建议替换为寄存器打印或 `NVIC_SystemReset()` 自动复位

### 3. 错误处理改进（中优先级）
> 文件：`firmware/app/src/main.c`、`firmware/drivers/uart/uart.c`

CubeMX 生成的错误处理全部是 `while(1)` 死循环，设备卡死无法定位问题。

- [ ] `main.c` 中 `Error_Handler()` 改为系统复位或错误日志记录
- [ ] `uart.c` 中 `MX_USART1_UART_Init()` 初始化失败时返回错误码，而非死循环

### 4. MSP 初始化清理（低优先级）
> 文件：`firmware/app/src/stm32f4xx_hal_msp.c`

- [ ] `HAL_MspInit()` 和 `HAL_MspDeInit()` 均为空函数，当前是死代码
- [ ] 后续添加外设时将 MSP 配置（GPIO 复用、DMA、中断）放在此文件中，目前可保留占位

### 5. 系统文件瘦身（低优先级）
> 文件：`firmware/platform/stm32f407/system/system_stm32f4xx.c`

- [ ] `SystemInit_ExtMemCtl()` 函数占 500+ 行，用于初始化外部 SRAM/SDRAM，但守护宏 `DATA_IN_ExtSRAM` / `DATA_IN_ExtSDRAM` 已被注释，代码永远不会编译
- [ ] 可直接删除该段死代码，或缩减为注释说明，提升文件可读性